import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const ProductPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default ProductPageLayout;